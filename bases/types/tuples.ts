(() => {
    // type that knows exactly how many elements it contains, and exactly which types it contains at specific positions.
     const hero: [string, number, boolean] = ['Iron Man', 100, true]
})()